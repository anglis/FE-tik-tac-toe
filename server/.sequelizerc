const path = require("path");

module.exports = {
  config: path.resolve("migration", "config.json"),
  "models-path": path.resolve("src", "models"),
  "seeders-path": path.resolve("migration", "seeders"),
  "migrations-path": path.resolve("migration", "migrations"),
};
